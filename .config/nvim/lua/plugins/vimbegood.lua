return {
  {
    "ThePrimeagen/vim-be-good",
    config = function()
        -- Optional: Add any configuration specific to vim-be-good here
        -- For example, setting up keybindings or custom commands if needed.
    end,
  }
}
