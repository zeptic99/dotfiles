return {
  "mbbill/undotree",
  cmd = "UndotreeToggle", -- Lazy load on command
  config = function()
    -- Optional configuration
    vim.g.undotree_WindowLayout = 3 -- Example: Set window layout
  end,
}
