context.properties = {
    # Allow sample rate switching
    default.clock.allowed-rates = [ 44100 48000 88200 96000 176400 192000 ]

    # Optional: default starting rate
    #default.clock.rate = 48000

    # Latency in samples (tweak if you get pops/clicks)
    default.clock.quantum = 1024
}
